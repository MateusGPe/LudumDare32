#include <SFML/Graphics.hpp>
#include <Box2D/Box2D.h>

using namespace sf;

enum playerState {
      STANDING
    , RUNNING
};

class player {
public:
    player();
    update();
private:
    Texture texMap;
    RectInt currentRec;
    b2Body* body;
    b2World* world;
}
